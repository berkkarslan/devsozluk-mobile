/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Launcher from './src/Launcher';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Launcher);
